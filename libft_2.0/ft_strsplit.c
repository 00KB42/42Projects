#include "libft.h"

char ** ft_strsplit(char const *s, char c)
{
    /*
    int i;
    int word_i;
    char **ptr;
    while (s[i]);
    for (i = 0, word_i = 0; s[word] == c; i++, word_i++)
        if (s[i] == c)
            ptr = (char**)malloc((word_i - 0) * sizeof(char));
        else
            for (i = 0; s[word] == c; i++)
    return (ptr);
    */
    return;
}
