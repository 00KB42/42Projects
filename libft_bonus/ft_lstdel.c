#include "libft.h"
/*void ft_lstdel(t_list **alst, void (*del)(void *, size_t))
{
    while (!(alst == NULL)) //basically until last link where it points to NULL;
    {
        del(*alst.content, *alst.content_size);
        free (*alst.content);
        alst = alst->next; // (move ot next link)
    }
 alst = NULL;
}
*/
